# Feel free to add as many definitions as you need here! Some examples with default fireball
init -1 python:
    # # fireball_img is defined here so it's accessible globally
    # fireball_img = "images/sprites/red_fireball.png"

    # # Registering the effects
    # register_animated("fast_fireball", fireball_img, frame_w=32, frame_h=32, cols=2, total_frames=2, movement_type="linear")
    
    # register_animated("curved_fireball", fireball_img, frame_w=32, frame_h=32, cols=2, total_frames=2, movement_type="cosine")

    # register_static("big_fireball_icon", fireball_img)